import React from 'react';
import product from './product';

const Prix = ({prix}) => {
    return (
        <div>
            <p>Prix : {product.prix}</p>
        </div>
    );
};

export default Prix;